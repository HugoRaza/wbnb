<h1 style="margin-left: 150px"><%= @vehicle.name %></h1>

<div class="container">
  <div class="card-vehicle">
    <img src= <%= @vehicle.image %> alt="vehicle">
    <p> <%= " #{@vehicle.price} €" %> </p>
    <p> <%= @vehicle.description %> </p>
    <p> <%= @vehicle.category %> </p>
    <p> <%= @vehicle.user.first_name %> </p>
    <% @vehicle.vehicle_reviews.each do |review| %>
      <div class="container">
        <div class="card-review">
          <h2>description</h2>
          <p><%= review.description %></p>
          <h2>rating</h2>
          <p><%= review.rating %></p>
          <h2>name</h2>
          <p><%= review.rental.user.first_name %></p>
          <img src=<%= review.rental.user.profile_picture %> alt="" style="width: 250px">
        </div>
      </div>
    <% end %>

  </div>
</div>
