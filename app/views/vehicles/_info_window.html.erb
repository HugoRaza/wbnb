<div class="card-window">
  <%= link_to vehicle.name, vehicle_path(vehicle) %>

  <div class="map-card-vehicle" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path vehicle.image %>)">
  </div>
  <%= "#{vehicle.price} €" %>
  <p> <%= vehicle.location %></p>
</div>
